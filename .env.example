# Leave empty when the frontend and backend share an origin or use the Vite proxy.
VITE_API_URL=/api
# Development proxy target (used only by vite.config.js).
VITE_DEV_BACKEND_URL=http://localhost:3000
